# 若本地仍保留小程序工程目录，勿加入 Git（嵌套 .git 会导致 GitHub Pages 部署 Checkout 失败）
wechat-program-englishstudy/

# Cloudflare Wrangler 本地缓存（勿提交，可能含账号信息）
**/.wrangler/

# 本地 npm 依赖（静态站不需要提交；git add -A 会误扫上万文件）
node_modules/

# Codex 本地页面渲染检查产物
.tmp-chrome*/
.tmp-chromedriver*/
travel/render-check.png
